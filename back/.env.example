# =====================================================
# Variables de entorno - Visor de PDFs
# =====================================================
#
# INSTRUCCIONES:
# 1. Copia este archivo como .env en el mismo directorio
# 2. Reemplaza todos los valores de ejemplo con tus credenciales reales
# 3. NUNCA subas el archivo .env a Git
# =====================================================

# Ruta base donde están los PDFs organizados
# Ejemplo Windows: D:/decadas o E:/documentos
# Ejemplo Linux: /var/data/pdfs
PDF_BASE_PATH=/ruta/a/tus/pdfs

# Credenciales de base de datos
DB_USER=tu_usuario_db
DB_PASSWORD=tu_password_seguro_aqui
DB_HOST=localhost
DB_PORT=3306
DB_NAME=digitalizacion

# Clave secreta para JWT (generar una clave aleatoria larga y segura)
# Puedes generar una con: openssl rand -base64 32
JWT_SECRET=cambia_esta_clave_por_una_aleatoria_muy_larga_y_segura
